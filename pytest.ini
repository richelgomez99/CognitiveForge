[pytest]
markers =
    slow: marks tests as slow (integration/E2E tests with LLM calls)
    e2e: marks tests as end-to-end tests
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers

